" deoplete 設定

" 使用 deoplete
" let g:deoplete#enable_at_startup = 1
" 設定每個檔案類型要用什麼提示，'_' 通用
" call deoplete#custom#option('sources', {
    " \ '_': ['buffer'],
    " \ 'php': ['buffer', 'phpactor'],
    " \})
